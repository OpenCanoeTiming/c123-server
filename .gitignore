node_modules/
dist/
*.log
.DS_Store
core

# Test XML files (large, not needed in repo)
*.xml

# Test config files (reference only)
user.config

# Downloaded test fixtures cache + installer runtime cache
.cache/

# Installer build artefacts
build-output/
installer/Output/
installer/iss-defines.iss
